[[source]]
url = "https://pypi.python.org/simple"
verify_ssl = true
name = "pypi"

[packages]
aiodocker = ">=0.13.0,<1.0"
tornado = ">=6.0.0,<7.0.0"
dataclasses = "*"

[dev-packages]
"flake8" = ">=3.0,<4.0"
mypy = ">=0.510,<1.0"

[requires]
python_version = "3.7"

[scripts]
test = "sh -c \"echo 'Running flake8' && flake8 && echo 'Running mypy' && mypy .\""
